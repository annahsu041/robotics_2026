# ============================================================
# Phase 2: Dynamic Trajectory Generation
# 階段二：動態軌跡生成 依賴套件
# ============================================================
#
# 安裝方式：
#   pip install -r requirements_phase2.txt
#
# 最低 Python 版本：3.8
# ============================================================

# 核心數學
numpy>=1.21.0
scipy>=1.7.0

# 凸優化（Week 5 核心）
cvxpy>=1.2.0

# 視覺化（本地測試用）
matplotlib>=3.4.0

# 測試框架
pytest>=7.0.0

# ROS 橋接（在 Ubuntu ROS 環境中由 apt 安裝，以下僅供參考）
# rospy  # apt install ros-noetic-rospy
# geometry_msgs  # apt install ros-noetic-geometry-msgs
# trajectory_msgs  # apt install ros-noetic-trajectory-msgs
